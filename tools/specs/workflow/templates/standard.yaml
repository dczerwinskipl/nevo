# Default Standard Specification Workflow template for repository initialization.
id: standard-v1
title: "Standard Specification Workflow"
type: standard
version: 1
steps:
  implementation:
    entryGates: []
    exitGates:
      - type: command
        action: test
      - type: human
        required: true
    finalize:
      - id: commit-and-push
    transitions:
      - to: verified
