# Default Small Specification Workflow template for repository initialization.
id: small-v1
title: "Small Specification Workflow"
type: small
version: 1
steps:
  implementation:
    status:
      active: implementing
      completed: implemented
    entryGates: []
    exitGates:
      - type: command
        action: test
    finalize:
      - id: commit-and-push
    transitions:
      - to: verified
