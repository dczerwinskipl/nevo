# Default Architectural Specification Workflow template for repository initialization.
id: architectural-v1
title: "Architectural Specification Workflow"
type: architectural
version: 1
steps:
  implementation:
    status:
      active: implementing
      completed: implemented
    entryGates: []
    exitGates:
      - type: command
        action: test
      - type: human
        required: true
    finalize:
      - id: commit-and-push
    transitions:
      - to: verified
