follow_ups:
  - id: FU-001
    source_task: finalization-hardening-and-migration
    kind: correctness
    severity: non-blocking
    reason: computeChangeFingerprint (tools/specs/service.mjs) only hashes
      overview.md + task graph shape; D7's Consequences text says
      change_fingerprint should also cover shared constraints, owner decisions,
      change-level acceptance criteria, and cross-task invariants.
      owner-decisions.md changes currently do not invalidate a spec review's
      fingerprint. tools/specs/service.mjs is outside task 09's allowed_paths so
      this could not be fixed here.
    resolver_task: null
    status: open
    resolution: null
  - id: FU-002
    source_task: conversational-approval-ergonomics
    kind: design-gap
    severity: non-blocking
    reason: "spec-approve.md D3 'Approve and start implementation' only performs the
      approve+start lifecycle transitions, then asks a second confirmation
      before actually implementing -- contradicts the option's own label and the
      'no repeated confirmation for an already-authorized action' principle
      (C8/D17). Fix: after start succeeds, the flow should continue directly
      into implementation in the same turn, without a further ask; 'Approve'
      alone (no start) should remain a hard stop with no implementation prompt."
    resolver_task: null
    status: open
    resolution: null
  - id: FU-003
    source_task: review-report-compaction-and-scope-exceptions
    kind: design-gap
    severity: non-blocking
    reason: "Tasks 01-11 declare no semantic_references block even though their own
      prose cites specific decisions/constraints/dependency contracts (D26/D29
      completeness check). D32 grandfathers this: the completeness check was
      only wired into /nevo-ai:spec-review by task 11, after tasks 01-10 were
      already approved, so it does not apply retroactively. Revisit only if any
      of tasks 01-11 is reopened."
    resolver_task: null
    status: open
    resolution: null
